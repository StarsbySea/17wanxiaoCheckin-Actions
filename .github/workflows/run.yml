name: 17wanxiaoCheckin

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
  schedule:
    - cron: 0 22,23 * * *

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Pip
      run: pip3 install requests pycryptodome
         
    - name: HealthyCheckIn
      run: |
        export http_proxy="http://llkhs:913782@155.94.128.124:8829"
        export https_proxy="llkhs:913782@155.94.128.124:8829"
        python3 index.py <<EOF
        ${{secrets.USERS}}
        EOF
        
